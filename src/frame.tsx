import { Grid, makeStyles } from "@material-ui/core";

export const Frame =() =>{
    const classes = useStyles();
    return(
        <>
            <Grid>
            </Grid>
            <Grid>
            </Grid>
        </>
    )
}
const useStyles = makeStyles(() => ({

}));